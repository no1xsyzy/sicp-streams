[tool.poetry]
name = "streams"
version = "0.1.0"
description = ""
authors = ["no1xsyzy <mf20070535@126.com>"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
